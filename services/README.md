# Simulated Services

Services emit OTel telemetry in AD-HOC mode and may share scenario execution logic with PRE-HOC.

The scenario source of truth is `configs/scenarios/`, not service-local hard-coded behavior.
